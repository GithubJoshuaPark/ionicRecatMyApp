import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobileCem.lguplus',
  appName: 'mobileCemLgUplus',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
